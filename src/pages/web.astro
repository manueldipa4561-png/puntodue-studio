---
import Site from '../layouts/Site.astro';
import Sim from '../components/Sim.astro';
import Chat from '../components/Chat.astro';
import Bi from '../components/Bi.astro';
const WA = `https://wa.me/393248423657?text=${encodeURIComponent('Ciao! Ho visto le vostre demo e vorrei parlarvi di un progetto.')}`;
const demos = [
  { slug: 'hair', name: 'SEZIONE', kind: 'Hair studio', line: 'Il sito di un salone, per lei e per lui.', tags: ['Lei / Lui', 'WebGL', 'Prenotazione'], next: ['Prenotazione con conferma su WhatsApp','Tessera fedeltà: ogni 6 servizi il 7° è in regalo', 'Coupon −15% sulla prima visita', 'Promemoria su WhatsApp prima della visita'] },
  { slug: 'innesto', name: 'INNESTO', kind: 'Ristrutturazioni', line: 'Ristrutturazioni chiavi in mano, con slider prima/dopo.', tags: ['Prima / Dopo', 'Case study', 'Sopralluogo'], next: ['Preventivo guidato in 4 domande', 'Area cliente con avanzamento del cantiere', 'Coupon sopralluogo gratuito a tempo', 'Sconto referral per chi consiglia lo studio'] },
  { slug: 'tattoo', name: 'Cultura Tattoo', kind: 'Tattoo studio', line: 'Tatuaggi su misura in bianco e nero, dal lucido alla pelle.', tags: ['Lettore di lucidi', 'Consulenza', 'Acconto'], next: ['Acconto online del 30% scalato dal totale', 'Promemoria WhatsApp 48 ore prima della seduta', 'Tessera a timbri: alla 5ª seduta −15%', 'Porta un amico: −10% a chi presenti'] },
];
const stats = [
  { t: 'Probabilità di vendere', a: { l: 'Nuovo contatto', v: 12.5, r: '5–20%' }, b: { l: 'Cliente già acquisito', v: 65, r: '60–70%' }, max: 100 },
  { t: 'Costo per ottenere un cliente', a: { l: 'Mantenerne uno', v: 1, r: '1×' }, b: { l: 'Trovarne uno nuovo', v: 15, r: '5–25×' }, max: 25 },
  { t: 'Profitto con +5% di clienti fedeli', a: { l: 'Oggi', v: 0, r: '—' }, b: { l: 'Con +5% di retention', v: 60, r: '+25–95%' }, max: 100 },
];
const feats = [
  { id: 'sito', t: 'Sito veloce', d: 'Più contatti ogni mese', n: 0.15, r: 0 },
  { id: 'fed', t: 'Tessera fedeltà', d: 'I clienti tornano più spesso', n: 0, r: 0.12 },
  { id: 'cp', t: 'Coupon', d: 'Riporta chi si era perso', n: 0.1, r: 0.05 },
  { id: 'wa', t: 'Promemoria WhatsApp', d: 'Meno appuntamenti saltati', n: 0, r: 0.06 },
  { id: 'ref', t: 'Referral', d: 'I clienti portano amici', n: 0.2, r: 0.02 },
];
const kstats = [
  { t: { it: 'Giovani USA incerti sul percorso', en: 'US young adults unsure about their path' },
    a: { l: { it: 'Incerti sulla carriera, 18–30', en: 'Unsure about career, 18–30' }, v: 66.7, r: { it: '≈ 2 su 3', en: '≈ 2 in 3' } },
    b: { l: { it: 'Gen Z, incerti sui lavori futuri', en: 'Gen Z, unsure about future jobs' }, v: 33.3, r: { it: '≈ 1 su 3', en: '≈ 1 in 3' } }, max: 100 },
  { t: { it: 'Le aziende dicono «competenze». Assumono altrimenti.', en: 'Employers say "skills". They hire otherwise.' },
    a: { l: { it: 'Assunzioni entry-level per competenze', en: 'Entry-level hires based on skills' }, v: 70, r: { it: '70%', en: '70%' } },
    b: { l: { it: "Assunti senza laurea dove l'hanno tolta", en: 'Hires without a degree where it was dropped' }, v: 0.14, r: { it: '< 1 su 700', en: '< 1 in 700' } }, max: 100 },
  { t: { it: 'Disoccupazione, giugno 2026', en: 'Unemployment, June 2026' },
    a: { l: { it: 'Tutti i lavoratori', en: 'All workers' }, v: 4, r: { it: '≈ 4%', en: '≈ 4%' } },
    b: { l: { it: 'Neolaureati, 22–27', en: 'Recent graduates, 22–27' }, v: 5.7, r: { it: '5,7%', en: '5.7%' } }, max: 10 },
];
---
<Site title="Lavori e demo — Punto Due Studio" description="Tre demo di siti complete e cliccabili, più KERN, un'app in concept che aiuta a scoprire cosa fare facendo lavoro vero." active="web">
      <section class="app" id="app">
        <div class="app-txt">
          <div class="ana-top">
            <span class="lab">/app · concept</span>
            <div class="k-seg" role="group" aria-label="Lingua del sito / Site language"><button type="button" data-ana-lang="it" aria-pressed="true">IT</button><button type="button" data-ana-lang="en" aria-pressed="false">EN</button></div>
          </div>
          <svg class="icon" viewBox="0 0 100 100" role="img" aria-label="KERN app icon">
            <rect width="100" height="100" rx="23" fill="#0F140E" />
            <g stroke="#E8E6DA"><path d="M23.13 76.87A38 38 0 0 1 76.87 23.13Z" fill="#E8E6DA" stroke="none" transform="translate(-5 -5)"/><path d="M23.13 76.87A38 38 0 0 0 76.87 23.13Z" fill="none" stroke-width="7" stroke-linejoin="round" transform="translate(5 5)"/><circle cx="66.4" cy="66.4" r="9" fill="#C9F24A" stroke="none"/></g>
          </svg>
          <h2 class="k-h">kern<span>.</span><sup style="font-size:.28em;letter-spacing:0;margin-left:.15em;vertical-align:super;opacity:.7">™</sup></h2>
          <p class="lead"><Bi it="Scopri cosa vuoi fare provandolo, non immaginandolo." en="Find out what you want to do by trying it, not imagining it." /></p>
          <ul class="k-tags" aria-label="In breve">
            <li><Bi it="Lavoro vero" en="Real work" /></li>
            <li><Bi it="Nessun cronometro" en="No timers" /></li>
            <li><Bi it="Vince chi lo fa meglio" en="Best work wins" /></li>
          </ul>
          <dl class="spec">
            <div><dt><Bi it="Piattaforme" en="Platforms" /></dt><dd>iOS · Android · Web app</dd></div>
            <div><dt><Bi it="Stato" en="Status" /></dt><dd><Bi it="Concept, in sviluppo" en="Concept, in development" /></dd></div>
            <div><dt><Bi it="Per chi" en="For" /></dt><dd><Bi it="Dai 18 ai 35 anni" en="Ages 18 to 35" /></dd></div>
          </dl>
        </div>

        <div class="k-wrap">
          <div class="k-ctl">
            <div class="k-seg" role="group" aria-label="Lingua"><button type="button" data-k-lang="en" aria-pressed="true">EN</button><button type="button" data-k-lang="it" aria-pressed="false">IT</button></div>
            <div class="k-seg" role="group" aria-label="Tema"><button type="button" data-k-theme="dark" aria-pressed="true">Dark</button><button type="button" data-k-theme="light" aria-pressed="false">Light</button></div>
          </div>
          <div class="phone" aria-label="Anteprima interattiva dell'app KERN">
            <div class="k-scr" id="kScr">
              <div class="k-load" id="kLoad" aria-hidden="true">
                <svg viewBox="0 0 100 100"><path d="M23.13 76.87A38 38 0 0 1 76.87 23.13Z" class="ks" fill="currentColor"/><path d="M23.13 76.87A38 38 0 0 0 76.87 23.13Z" fill="none" stroke-width="7" stroke-linejoin="round" class="kl" stroke="currentColor"/><circle cx="66.4" cy="66.4" r="9" class="kc"/></svg>
                <div class="k-word">kern<i>.</i></div>
                <div class="k-tag"><span>Follow the trail.</span><strong>Find your<b>KERN</b>.</strong></div>
              </div>
              <div class="k-app">
                <div class="k-top">
                  <div class="k-logo"><svg viewBox="0 0 100 100" aria-hidden="true"><path d="M23.13 76.87A38 38 0 0 1 76.87 23.13Z" fill="currentColor" transform="translate(-5 -5)"/><path d="M23.13 76.87A38 38 0 0 0 76.87 23.13Z" fill="none" stroke-width="7" stroke-linejoin="round" stroke="currentColor" transform="translate(5 5)"/><circle cx="66.4" cy="66.4" r="9"/></svg>kern<i>.</i></div>
                  <span class="k-pill">1 of 3 done</span>
                </div>
                <div class="k-body">
                  <div class="k-pane on" id="k-missions">
                    <div><div class="k-l">All missions open</div><h4>Pick your<br>next <b>move.</b></h4></div>
                    <div class="k-card k-main"><div class="k-l">Mission 002 · startup partner</div><h5>Fix what already exists</h5><p>Make a real startup's onboarding screen clearer. The opposite of starting from a blank page.</p><div class="k-l">128 answers · judged on quality</div><button type="button" class="k-btn" data-k-toast="Your answer is saved. Add another one anytime.">Add your answer</button></div>
                    <div class="k-card"><div class="k-l">Mission 003 · KERN</div><h5>Rewrite a confusing email</h5><p>Do it your way. Every answer counts.</p><div class="k-l">54 answers</div></div>
                    <div class="k-card"><div class="k-l">Mission 004 · studio client</div><h5>Work with a partner</h5><p>Answer together with someone else.</p><div class="k-l">31 answers</div></div>
                  </div>
                  <div class="k-pane" id="k-yourkern">
                    <div><div class="k-l">yourKERN · first signs</div><h4>Your <b>KERN</b><br>so far.</h4></div>
                    <div class="k-card"><div class="k-l">Time on KERN missions</div><div class="k-stat"><b>1h 15m</b><span class="k-l">this week · only you see this</span></div><div class="k-bar"><i></i></div></div>
                    <div class="k-card"><div class="k-l">What you seem drawn to</div><div class="k-chips"><span>Shaping ideas</span><span>Writing</span><span>Solo work</span></div></div>
                    <div class="k-card"><div class="k-l">A first guess</div><div class="k-sig">You seemed to light up on the open brief and drag on the polish.</div><div class="k-l">Why we think so: you tried 3 versions of the open brief, and you wrote "time flew".</div>
                      <div class="k-ask"><button type="button" class="k-yes" data-k-msg="Saved. This sharpens your KERN.">That feels right</button><button type="button" data-k-msg="Noted. We'll give this guess less weight.">Not really</button></div><div class="k-l k-fb" id="kFb" aria-live="polite"></div></div>
                    <div><div class="k-l">Completed</div><div class="k-done"><span>Make somebody care</span><small>MISSION 001</small></div></div>
                    <div class="k-card"><div class="k-l">Worth trying next</div><h5>Improve an existing product page</h5><p>Tests the part you seemed to drag on.</p></div>
                  </div>
                  <div class="k-pane" id="k-trail">
                    <div><div class="k-l">Your Trail</div><h4>Follow the<br><b>trail.</b></h4></div>
                    <div class="k-tr">
                      <div class="k-st fin"><h5>Make somebody care</h5><p>Build from zero · done</p></div>
                      <div class="k-st now"><h5>Fix what already exists</h5><p>Improve what exists · in progress</p></div>
                      <div class="k-st"><h5>Work with a partner</h5><p>Pair up · not started</p></div>
                      <div class="k-st"><h5>Your Kern card</h5><p>What we saw, why, and your next move</p></div>
                    </div>
                    <div class="k-card"><div class="k-l">Leave a sign</div><p>After each mission, leave one short tip for the next person.</p></div>
                  </div>
                </div>
                <div class="k-toast" id="kToast" role="status" aria-live="polite"></div>
                <nav class="k-tabs" aria-label="KERN">
                  <button type="button" class="on" data-k-tab="missions">Missions</button>
                  <button type="button" data-k-tab="yourkern">your<span>KERN</span></button>
                  <button type="button" data-k-tab="trail">Trail</button>
                </nav>
              </div>
            </div>
          </div>
          <button type="button" class="k-replay" id="kReplay">↻ Rivedi l'apertura</button>
        </div>
      </section>

      <section class="why" id="perche">
        <span class="lab"><Bi it="/perché" en="/why" /></span>
        <h2><Bi it="Chi non sa cosa vuole non trova una porta d'ingresso." en="If you don't know what you want, there's no way in." /></h2>
        <p class="lead"><Bi it="Giovani incerti, aziende che chiedono prove, sempre meno posti d'ingresso." en="Unsure young people, employers asking for proof, fewer entry-level roles." /></p>
        <div class="charts">
          {kstats.map((s) => (
            <figure class="chart">
              <figcaption><Bi it={s.t.it} en={s.t.en} /></figcaption>
              {[s.a, s.b].map((r, k) => (
                <div class="row">
                  <span><Bi it={r.l.it} en={r.l.en} /></span>
                  <b><Bi it={r.r.it} en={r.r.en} /></b>
                  <div class="track"><i class={k ? 'y' : 'w'} style={`--v:${Math.max((r.v / s.max) * 100, 1.5)}%`}></i></div>
                </div>
              ))}
            </figure>
          ))}
        </div>
        <p class="note"><Bi it='Dati di ricerca generali, non risultati di KERN; barre indicative, alcune cifre da fonti secondarie. Fonti: NACE Job Outlook 2026 · Tallo / Forbes 2025 · Big Brothers Big Sisters / Harris Poll 2025 · Harvard Business School e Burning Glass Institute · NPR e IBISWorld 2026. <a href="#fonti">Dettagli</a>' en='General research, not KERN results; bars are indicative and some figures come from secondary sources. Sources: NACE Job Outlook 2026 · Tallo / Forbes 2025 · Big Brothers Big Sisters / Harris Poll 2025 · Harvard Business School &amp; Burning Glass Institute · NPR &amp; IBISWorld 2026. <a href="#fonti">Details</a>' /></p>
        <div class="duo">
          <div><span class="lab"><Bi it="La differenza" en="The difference" /></span><p><Bi it="Gli altri partono da un ruolo. KERN parte da te." en="Others start from a role. KERN starts from you." /></p></div>
          <div><span class="lab"><Bi it="L'obiettivo" en="The goal" /></span><p><Bi it="Aiutarti a capire cosa vuoi, con lavoro vero." en="Help you work out what you want, through real work." /></p></div>
        </div>
      </section>


      <section class="top">
        <span class="lab">/web</span>
        <h1>Nuovo link.</h1>
        <p class="lead">Tre siti da aprire e toccare con mano. Un click e sei dentro.</p>
      </section>

      <section class="demos" aria-label="Demo">
        {demos.map((d, i) => (
          <article class="card">
            <div class="shot">
              <div class="bar" aria-hidden="true"><i></i><i></i><i></i><span>{d.slug === 'hair' ? 'sezione.it' : d.slug === 'innesto' ? 'innesto.it' : 'culturatattoo.it'}</span></div>
              <div class="view" data-shot>
                <img class="poster" src={`/previews/${d.slug}.webp`} width="960" height="600" alt="" loading="lazy" decoding="async" />
                <iframe data-src={`/demo/${d.slug}/index.html`} title={`Anteprima ${d.name}`} scrolling="no" tabindex="-1" aria-hidden="true"></iframe>
                <a class="cover" href={`/demo/${d.slug}/index.html`} target="_blank" rel="noopener" aria-label={`Apri la demo ${d.name}`}><span>Apri la demo →</span></a>
              </div>
            </div>
            <div class="meta">
              <span class="lab">0{i + 1} · {d.kind}</span>
              <h3>{d.name}</h3>
              <p>{d.line}</p>
              <ul class="tags">{d.tags.map((t) => <li>{t}</li>)}</ul>
              <div class="next">
                <span class="lab">Prossime modifiche</span>
                <ul>{d.next.map((n) => <li>{n}</li>)}</ul>
              </div>
              <a class="lnk" href={`/demo/${d.slug}/index.html`} target="_blank" rel="noopener">Apri la demo →</a>
            </div>
          </article>
        ))}
      </section>
      <section class="why" id="numeri">
        <span class="lab">/numeri</span>
        <h2>Far tornare un cliente vale di più.</h2>
        <p class="lead">Fedeltà e coupon servono a questo. Dati di ricerca generali, non risultati delle demo.</p>
        <div class="charts">
          {stats.map((s) => (
            <figure class="chart">
              <figcaption>{s.t}</figcaption>
              {[s.a, s.b].map((r, k) => (
                <div class="row">
                  <span>{r.l}</span>
                  <b>{r.r}</b>
                  <div class="track"><i class={k ? 'y' : 'w'} style={`--v:${(r.v / s.max) * 100}%`}></i></div>
                </div>
              ))}
            </figure>
          ))}
        </div>
        <p class="note">Le barre mostrano il centro dell'intervallo indicato dalle fonti.</p>
      </section>

      <Sim feats={feats} kpi={['Clienti attivi al 12° mese', 'Clienti che tornano', 'Nuovi clienti al mese']} heading="Accendi le funzioni." lead="Aggiungine una alla volta e guarda come cambiano i clienti in un anno." />

      <section class="cta-s">
        <Chat kind="sent" />
        <div class="cta">
          <a class="btn" href={WA} target="_blank" rel="noopener">Scrivici <span aria-hidden="true">→</span></a>
          <a class="lnk" href="mailto:projects@puntoduestudio.it">projects@puntoduestudio.it</a>
        </div>
      </section>

      <section class="src" id="fonti">
        <span class="lab">/fonti</span>
        <ol>
          <li>Amy Gallo, «The Value of Keeping the Right Customers», Harvard Business Review, ottobre 2014: aumentare la retention del 5% aumenta i profitti del 25–95%; acquisire un cliente costa 5–25 volte più che mantenerlo (dati di Fred Reichheld, Bain &amp; Company). <a href="https://hbr.org/2014/10/the-value-of-keeping-the-right-customers" target="_blank" rel="noopener">hbr.org</a></li>
          <li>P. Farris, N. Bendle, P. Pfeifer, D. Reibstein, <i>Marketing Metrics: The Definitive Guide to Measuring Marketing Performance</i>, Pearson, 2010: probabilità di vendita 5–20% a un nuovo contatto, 60–70% a un cliente esistente.</li>
          <li>Forbes su rapporto Tallo, maggio 2025, e Big Brothers Big Sisters / Harris Poll, 2025: incertezza dei giovani adulti sul percorso di carriera. <a href="https://www.forbes.com/sites/juliakorn/2025/05/20/new-report-50-of-young-adults-feel-isolated-in-career-planning/" target="_blank" rel="noopener">forbes.com</a> · <a href="https://www.bbbs.org/2025/09/career-confidence-in-crisis-new-report-from-big-brothers-big-sisters-shows-gen-zers-lack-critical-mentorship/" target="_blank" rel="noopener">bbbs.org</a></li>
          <li>NACE Job Outlook 2026 (70% usa assunzioni per competenze nei ruoli entry-level); TestGorilla 2025; Harvard Business School e Burning Glass Institute (meno di 1 nuovo assunto su 700 senza laurea nelle aziende che l'hanno tolta). Riassunti secondari: <a href="https://blog.theinterviewguys.com/the-state-of-skills-based-hiring/" target="_blank" rel="noopener">The Interview Guys</a> · <a href="https://testlify.com/skills-based-hiring-statistics/" target="_blank" rel="noopener">Testlify</a> · <a href="https://www.forbes.com/sites/terdawn-deboe/2026/08/10/employers-are-hiring-for-skills-over-degrees-now-ai-is-why/" target="_blank" rel="noopener">Forbes, agosto 2026</a></li>
          <li>NPR, agosto 2026 e IBISWorld: disoccupazione dei neolaureati (5,7% a giugno 2026) e mercato entry-level. <a href="https://www.npr.org/2026/08/18/nx-s1-5910677/recent-college-graduates-employment-job-artificial-intelligence" target="_blank" rel="noopener">npr.org</a> · <a href="https://www.ibisworld.com/blog/2026-job-market-leaves-new-grads-behind/1/1126/" target="_blank" rel="noopener">ibisworld.com</a></li>
          <li>Le demo INNESTO, SEZIONE e Cultura Tattoo sono concept realizzati da Punto Due Studio; i nomi e le attività sono fittizi. L'app KERN è un concept non ancora testato con utenti: schermate, nomi e numeri sono d'esempio.</li>
        </ol>
      </section>
</Site>

<style is:global>
      .top { padding-bottom: 24px; border-top: 1px solid rgba(255,255,255,.25); }
      .demos { display: grid; gap: 28px; grid-template-columns: repeat(auto-fit, minmax(min(100%, 340px), 1fr)); padding-top: 24px; }
      .card { display: flex; flex-direction: column; gap: 18px; }
      .shot { border-radius: 16px; overflow: hidden; border: 1px solid rgba(255,255,255,.35); background: var(--blue); transition: border-color .25s, transform .35s cubic-bezier(.2,.7,.2,1); }
      .shot:hover { border-color: var(--yellow); transform: translateY(-4px); }
      .bar { display: flex; align-items: center; gap: 7px; padding: 10px 14px; border-bottom: 1px solid rgba(255,255,255,.3); }
      .bar i { width: 9px; height: 9px; border-radius: 50%; background: var(--yellow); }
      .bar i:nth-child(2) { background: #fff; } .bar i:nth-child(3) { background: transparent; box-shadow: inset 0 0 0 1.5px #fff; }
      .bar span { margin-left: 10px; font: 500 11px/1 var(--mono); letter-spacing: .1em; color: rgba(255,255,255,.75); }
      .view { position: relative; aspect-ratio: 16 / 10; overflow: hidden; background: #0a0a0a; }
      .view iframe, .view .poster { position: absolute; left: 0; top: 0; width: 1280px; height: 800px; border: 0; transform: scale(var(--s, .3)); transform-origin: 0 0; pointer-events: none; }
      .view .poster { object-fit: cover; }
      .view iframe { background: #fff; opacity: 0; transition: opacity .5s; }
      .view iframe.ready { opacity: 1; }
      .cover { position: absolute; inset: 0; display: flex; align-items: flex-end; justify-content: flex-end; padding: 14px; background: linear-gradient(transparent 55%, rgba(0,52,214,.55)); transition: background .3s; }
      .cover span { background: var(--yellow); color: var(--blue); font-weight: 700; font-size: .95rem; padding: 10px 16px; border-radius: 999px; opacity: 0; transform: translateY(8px); transition: opacity .25s, transform .3s cubic-bezier(.2,.7,.2,1); }
      .shot:hover .cover span, .cover:focus-visible span { opacity: 1; transform: none; }
      .next { margin: 0 0 18px; padding-top: 14px; border-top: 1px solid rgba(255,255,255,.28); }
      .next ul { list-style: none; margin: 12px 0 0; padding: 0; display: grid; gap: 8px; font-size: .95rem; line-height: 1.35; color: rgba(255,255,255,.88); }
      .next li { display: grid; grid-template-columns: 1.1em 1fr; }
      .next li::before { content: '+'; font: 700 .95rem var(--mono); color: var(--yellow); }
      .meta h3 { margin: 10px 0 8px; font-size: 1.9rem; }
      .meta p { margin: 0 0 14px; color: rgba(255,255,255,.85); line-height: 1.45; }
      .tags { display: flex; flex-wrap: wrap; gap: 8px; list-style: none; margin: 0 0 16px; padding: 0; }
      .tags li { font: 500 11px/1 var(--mono); letter-spacing: .08em; text-transform: uppercase; padding: 7px 10px; border: 1px solid rgba(255,255,255,.35); border-radius: 999px; }
      .app { display: grid; gap: clamp(32px, 6vw, 80px); grid-template-columns: 1fr; align-items: center; }
      .icon { width: 96px; height: 96px; display: block; margin: 22px 0 6px; filter: drop-shadow(0 10px 24px rgba(0,0,0,.25)); }
      .k-h { margin-top: 6px; letter-spacing: -.07em; } .k-h span { color: #C9F24A; letter-spacing: 0; }
      .spec { margin: 26px 0 0; border-top: 1px solid rgba(255,255,255,.28); max-width: 34rem; }
      .spec div { display: flex; justify-content: space-between; gap: 16px; padding: 12px 0; border-bottom: 1px solid rgba(255,255,255,.28); }
      .spec dt { font: 500 12px/1.6 var(--mono); letter-spacing: .08em; text-transform: uppercase; color: var(--yellow); }
      .spec dd { margin: 0; font-weight: 600; text-align: right; }
      .feat { list-style: none; margin: 24px 0 0; padding: 0; display: grid; gap: 14px; max-width: 34rem; }
      .feat b { display: block; font-size: 1.1rem; letter-spacing: -.01em; }
      .feat span { color: rgba(255,255,255,.8); }

      /* KERN phone: own palette (night / bone / lime) */
      .k-wrap { display: grid; justify-items: center; gap: 16px; }
      .k-ctl { display: flex; gap: 10px; flex-wrap: wrap; justify-content: center; }
      .k-seg { display: inline-flex; border: 1px solid rgba(255,255,255,.4); border-radius: 999px; overflow: hidden; }
      .k-seg button { background: none; border: 0; color: #fff; padding: 5px 11px; font: 600 .7rem 'Inter Tight Variable', system-ui, sans-serif; cursor: pointer; }
      .k-seg button[aria-pressed="true"] { background: var(--yellow); color: #000; }
      .k-replay { background: none; border: 1px solid rgba(255,255,255,.4); color: #fff; border-radius: 999px; padding: 9px 16px; font: 600 .85rem 'Inter Tight Variable', system-ui, sans-serif; cursor: pointer; }
      .k-replay:hover { border-color: var(--yellow); color: var(--yellow); }
      .k-scr.light { --bg: #E8E6DA; --fg: #141A12; --acc: #2C5A3A; --on: #E8E6DA; --line: #B9B7A8; --dim: #5b6156; }
      .k-scr { --bg: #0F140E; --fg: #E8E6DA; --acc: #C9F24A; --on: #0F140E; --line: #2a3128; --dim: #9a9d8e; position: relative; height: 600px; border-radius: 34px; overflow: hidden; background: var(--bg); color: var(--fg); }
      .k-scr h4, .k-scr h5, .k-scr p { margin: 0; }
      .k-scr button { font-family: inherit; }
      .k-app { height: 100%; display: flex; flex-direction: column; }
      .k-top { padding: 20px 18px 6px; display: flex; justify-content: space-between; align-items: center; }
      .k-logo { display: flex; align-items: center; gap: 6px; font-weight: 800; font-size: 21px; letter-spacing: -.07em; }
      .k-logo svg { width: 22px; } .k-logo circle { fill: var(--acc); } .k-logo i { font-style: normal; color: var(--acc); letter-spacing: 0; }
      .k-pill { font: 400 10px var(--mono); border: 1px solid var(--line); border-radius: 999px; padding: 5px 10px; color: var(--dim); }
      .k-body { flex: 1; overflow: auto; padding: 10px 18px 12px; scrollbar-width: none; }
      .k-body::-webkit-scrollbar { display: none; }
      .k-pane { display: none; flex-direction: column; gap: 12px; }
      .k-pane.on { display: flex; }
      .k-l { font: 400 10px var(--mono); color: var(--dim); letter-spacing: .04em; text-transform: uppercase; }
      .k-scr h4 { margin-top: 6px; font-weight: 800; font-size: 28px; letter-spacing: -.05em; line-height: 1; } .k-scr h4 b { color: var(--acc); }
      .k-card { border: 1.5px solid var(--fg); border-radius: 14px; padding: 13px; display: flex; flex-direction: column; gap: 8px; }
      .k-card h5 { font-weight: 800; font-size: 18px; letter-spacing: -.04em; line-height: 1.1; }
      .k-card p { font-size: 13px; line-height: 1.4; opacity: .82; }
      .k-main { background: var(--fg); color: var(--bg); } .k-main .k-l { color: var(--bg); opacity: .65; } .k-main p { color: var(--bg); }
      .k-btn { align-self: flex-start; border: 0; border-radius: 999px; padding: 9px 16px; font-weight: 800; font-size: 13px; cursor: pointer; background: var(--bg); color: var(--acc); }
      .k-stat { display: flex; align-items: baseline; gap: 8px; flex-wrap: wrap; } .k-stat b { font-weight: 800; font-size: 36px; letter-spacing: -.06em; line-height: 1; }
      .k-bar { height: 6px; border-radius: 6px; background: var(--line); overflow: hidden; } .k-bar i { display: block; height: 100%; width: 62%; background: var(--acc); }
      .k-chips { display: flex; flex-wrap: wrap; gap: 6px; } .k-chips span { border: 1px solid var(--fg); border-radius: 999px; padding: 5px 11px; font-size: 12px; font-weight: 500; }
      .k-sig { border-left: 3px solid var(--acc); padding-left: 12px; font-size: 14px; line-height: 1.4; }
      .k-ask { display: flex; gap: 8px; flex-wrap: wrap; }
      .k-ask button { font-size: 12px; font-weight: 800; border: 1.5px solid var(--fg); background: none; color: var(--fg); border-radius: 999px; padding: 7px 12px; cursor: pointer; }
      .k-ask .k-yes { background: var(--acc); color: var(--on); border-color: var(--acc); }
      .k-fb { min-height: 1.2em; color: var(--acc); }
      .k-done { display: flex; justify-content: space-between; font-size: 14px; padding: 9px 0; border-bottom: 1px solid var(--line); } .k-done small { font: 400 10px var(--mono); color: var(--dim); }
      .k-tr { position: relative; display: flex; flex-direction: column; gap: 18px; padding-left: 32px; }
      .k-tr::before { content: ''; position: absolute; left: 10px; top: 8px; bottom: 8px; width: 2px; background: var(--line); }
      .k-st { position: relative; } .k-st::before { content: ''; position: absolute; left: -30px; top: 2px; width: 16px; height: 16px; border-radius: 50%; border: 2px solid var(--fg); background: var(--bg); }
      .k-st.fin::before { background: var(--fg); } .k-st.now::before { background: var(--acc); border-color: var(--acc); }
      .k-st h5 { font-weight: 800; font-size: 16px; letter-spacing: -.03em; } .k-st p { font-size: 12px; color: var(--dim); margin-top: 2px; }
      .k-toast { position: absolute; left: 14px; right: 14px; bottom: 72px; background: var(--fg); color: var(--bg); border-radius: 12px; padding: 10px 12px; font-size: 12px; font-weight: 600; opacity: 0; transform: translateY(8px); transition: opacity .25s, transform .25s; pointer-events: none; }
      .k-toast.on { opacity: 1; transform: none; }
      .k-tabs { display: flex; border-top: 1px solid var(--line); padding: 6px 8px 12px; }
      .k-tabs button { flex: 1; background: none; border: 0; color: var(--dim); font-weight: 800; font-size: 12px; letter-spacing: -.01em; display: flex; flex-direction: column; align-items: center; gap: 5px; cursor: pointer; padding: 6px 0; }
      .k-tabs button::before { content: ''; width: 6px; height: 6px; border-radius: 50%; background: transparent; }
      .k-tabs button.on { color: var(--fg); } .k-tabs button.on::before { background: var(--acc); } .k-tabs button.on span { color: var(--acc); }
      /* launch loader (plays once when the phone scrolls into view) */
      .k-load { position: absolute; inset: 0; z-index: 3; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 22px; background: var(--bg); transition: opacity .5s; }
      .k-load.off { opacity: 0; pointer-events: none; }
      .k-load svg { width: 92px; overflow: visible; color: var(--fg); }
      .ks, .kl { transform: translate(0,0); } .kl { fill: none; }
      .kc { transform-box: fill-box; transform-origin: center; transform: scale(0); fill: var(--acc); }
      .k-word { font-weight: 800; font-size: 46px; letter-spacing: -.07em; line-height: 1; opacity: 0; transform: translateY(8px); } .k-word i { font-style: normal; color: var(--acc); letter-spacing: 0; }
      .k-tag { position: absolute; bottom: 44px; text-align: center; display: flex; flex-direction: column; gap: 6px; opacity: 0; }
      .k-tag span { font-weight: 500; font-size: 16px; letter-spacing: -.02em; line-height: 1; opacity: .6; }
      .k-tag strong { font-weight: 800; font-size: 27px; letter-spacing: -.06em; line-height: 1; } .k-tag b { color: var(--acc); }
      .k-load.go .ks { animation: kS .9s .35s cubic-bezier(.65,0,.25,1) forwards; }
      .k-load.go .kl { animation: kL .9s .35s cubic-bezier(.65,0,.25,1) forwards; }
      .k-load.go .kc { animation: kC .5s 1.05s cubic-bezier(.3,1.6,.5,1) forwards; }
      .k-load.go .k-word { animation: kR .6s 1.2s cubic-bezier(.2,.8,.2,1) forwards; }
      .k-load.go .k-tag { animation: kR .6s 1.6s cubic-bezier(.2,.8,.2,1) forwards; }
      @keyframes kS { to { transform: translate(-5px,-5px); } } @keyframes kL { to { transform: translate(5px,5px); } }
      @keyframes kC { to { transform: scale(1); } } @keyframes kR { to { opacity: 1; transform: none; } }
      @media (prefers-reduced-motion: reduce) { .k-load { display: none; } .k-toast { transition: none; } }

      /* why + difference */
      .why { border-top: 1px solid rgba(255,255,255,.25); }
      .charts { display: grid; gap: 22px; margin-top: 40px; grid-template-columns: repeat(auto-fit, minmax(min(100%, 320px), 1fr)); }
      .chart { margin: 0; padding: 22px; border: 1px solid rgba(255,255,255,.3); border-radius: 18px; background: rgba(0,52,214,.55); }
      .chart figcaption { font-weight: 700; font-size: 1.15rem; letter-spacing: -.02em; margin-bottom: 20px; }
      .row { display: grid; grid-template-columns: 1fr auto; gap: 6px 12px; margin-bottom: 16px; font-size: .92rem; }
      .row .track { grid-column: 1 / -1; height: 14px; border-radius: 999px; background: rgba(255,255,255,.14); overflow: hidden; }
      .row b { font: 700 .95rem var(--mono); }
      .row i { display: block; height: 100%; width: 0; border-radius: 999px; transition: width 1.1s cubic-bezier(.2,.7,.2,1); }
      .row i.w { background: #fff; } .row i.y { background: var(--yellow); }
      .chart.in .row i { width: var(--v); }
      .k-tags { list-style: none; margin: 26px 0 0; padding: 0; display: flex; flex-wrap: wrap; gap: 10px; }
      .k-tags li { border: 1px solid rgba(255,255,255,.4); border-radius: 999px; padding: 9px 16px; font-weight: 600; font-size: .95rem; background: rgba(255,255,255,.06); }
      .k-wrap > * { position: relative; z-index: 1; }
      .k-wrap::before { content: ''; position: absolute; inset: 6% -14%; background: radial-gradient(closest-side, rgba(255,224,0,.24), transparent 72%); filter: blur(28px); pointer-events: none; }
      .duo { display: grid; gap: 22px; margin-top: 40px; grid-template-columns: repeat(auto-fit, minmax(min(100%, 300px), 1fr)); }
      .duo > div { padding: 26px; border-radius: 18px; background: rgba(255,224,0,.12); border: 1px solid rgba(255,224,0,.45); }
      .duo p { margin: 14px 0 0; font-weight: 700; font-size: clamp(1.3rem, 2.4vw, 1.8rem); letter-spacing: -.03em; line-height: 1.15; }
      .ana-top { display: flex; justify-content: flex-start; margin-bottom: 4px; align-items: center; gap: 18px; flex-wrap: wrap; }
      html:not([data-ana="en"]) span[lang="en"], html[data-ana="en"] span[lang="it"] { display: none; }
      .note a { color: inherit; text-decoration: underline; }
      @media (min-width: 1024px) { .app { grid-template-columns: 1.1fr .9fr; } }
      @media (prefers-reduced-motion: reduce) { .row i { transition: none; } }
</style>

<script>
      document.querySelectorAll<HTMLElement>('[data-shot]').forEach((el) => {
        const fit = () => el.style.setProperty('--s', String(el.clientWidth / 1280));
        fit();
        new ResizeObserver(fit).observe(el);
      });

      // Anteprime live solo su desktop con mouse e solo vicino allo schermo; altrove resta il poster leggero.
      if (matchMedia('(min-width: 1024px) and (hover: hover)').matches) {
        const live = new IntersectionObserver((es) => es.forEach((e) => {
          if (!e.isIntersecting) return;
          live.unobserve(e.target);
          const f = e.target.querySelector<HTMLIFrameElement>('iframe')!;
          f.addEventListener('load', () => f.classList.add('ready'), { once: true });
          f.src = f.dataset.src!;
        }), { rootMargin: '250px' });
        document.querySelectorAll('[data-shot]').forEach((v) => live.observe(v));
      }

      const io = new IntersectionObserver((es) => es.forEach((e) => { if (e.isIntersecting) { e.target.classList.add('in'); io.unobserve(e.target); } }), { threshold: 0.35 });
      document.querySelectorAll('.chart').forEach((c) => io.observe(c));
      const kScr = document.getElementById('kScr')!;
      const kLoad = document.getElementById('kLoad')!;
      const still = matchMedia('(prefers-reduced-motion: reduce)').matches;
      let kT = 0;
      const play = () => {
        clearTimeout(kT);
        kLoad.classList.remove('go', 'off'); void kLoad.offsetWidth; kLoad.classList.add('go');
        kT = window.setTimeout(() => kLoad.classList.add('off'), 3400);
      };
      if (still) kLoad.classList.add('off');
      else new IntersectionObserver((es, o) => { if (es[0].isIntersecting) { play(); o.disconnect(); } }, { threshold: 0.5 }).observe(kScr);
      document.getElementById('kReplay')!.addEventListener('click', () => { if (!still) play(); });

      const tabs = kScr.querySelectorAll<HTMLElement>('[data-k-tab]');
      tabs.forEach((t) => t.addEventListener('click', () => {
        tabs.forEach((x) => x.classList.toggle('on', x === t));
        kScr.querySelectorAll('.k-pane').forEach((p) => p.classList.toggle('on', p.id === 'k-' + t.dataset.kTab));
      }));
      const toast = document.getElementById('kToast')!;
      let tT = 0;
      kScr.querySelectorAll<HTMLElement>('[data-k-toast]').forEach((b) => b.addEventListener('click', () => {
        toast.textContent = tr(b.dataset.kToast!); toast.classList.add('on');
        clearTimeout(tT); tT = window.setTimeout(() => toast.classList.remove('on'), 2200);
      }));
      const fb = document.getElementById('kFb')!;
      kScr.querySelectorAll<HTMLElement>('[data-k-msg]').forEach((b) => b.addEventListener('click', () => { fb.textContent = tr(b.dataset.kMsg!); fb.dataset.src = b.dataset.kMsg!; }));
      const IT: Record<string, string> = {
        'Follow the trail.': 'Segui la traccia.',
        'Find your<b>KERN</b>.': 'Trova il tuo <b>KERN</b>.',
        '1 of 3 done': '1 su 3 fatte',
        'All missions open': 'Tutte le missioni aperte',
        'Pick your<br>next <b>move.</b>': 'Scegli la<br>prossima <b>mossa.</b>',
        'Mission 002 · startup partner': 'Missione 002 · startup partner',
        'Fix what already exists': 'Sistema ciò che esiste già',
        "Make a real startup's onboarding screen clearer. The opposite of starting from a blank page.": "Rendi più chiara la schermata di onboarding di una vera startup. L'opposto di partire da una pagina bianca.",
        '128 answers · judged on quality': '128 risposte · giudicate sulla qualità',
        'Add your answer': 'Aggiungi la tua risposta',
        'Mission 003 · KERN': 'Missione 003 · KERN',
        'Rewrite a confusing email': "Riscrivi un'email confusa",
        'Do it your way. Every answer counts.': 'Fallo a modo tuo. Ogni risposta conta.',
        '54 answers': '54 risposte',
        'Mission 004 · studio client': 'Missione 004 · cliente dello studio',
        'Work with a partner': 'Lavora con un partner',
        'Answer together with someone else.': 'Rispondi insieme a qualcun altro.',
        '31 answers': '31 risposte',
        'Missions': 'Missioni',
        'yourKERN · first signs': 'yourKERN · primi segnali',
        'Your <b>KERN</b><br>so far.': 'Il tuo <b>KERN</b><br>finora.',
        'Time on KERN missions': 'Tempo sulle missioni KERN',
        'this week · only you see this': 'questa settimana · lo vedi solo tu',
        'What you seem drawn to': 'Cosa sembra attirarti',
        'Shaping ideas': 'Dare forma alle idee', 'Writing': 'Scrittura', 'Solo work': 'Lavoro in solitaria',
        'A first guess': 'Una prima ipotesi',
        'You seemed to light up on the open brief and drag on the polish.': 'Ti sei acceso sul brief aperto e hai faticato sulla rifinitura.',
        'Why we think so: you tried 3 versions of the open brief, and you wrote "time flew".': 'Perché lo pensiamo: hai provato 3 versioni del brief aperto e hai scritto "il tempo è volato".',
        'That feels right': 'Mi ci ritrovo', 'Not really': 'Non proprio',
        'Completed': 'Completate', 'Make somebody care': 'Fai importare a qualcuno', 'MISSION 001': 'MISSIONE 001',
        'Worth trying next': 'Da provare dopo', 'Improve an existing product page': 'Migliora la pagina di un prodotto esistente',
        'Tests the part you seemed to drag on.': 'Mette alla prova la parte in cui hai faticato.',
        'Your Trail': 'La tua traccia', 'Follow the<br><b>trail.</b>': 'Segui la<br><b>traccia.</b>',
        'Build from zero · done': 'Costruisci da zero · fatta', 'Improve what exists · in progress': 'Migliora ciò che esiste · in corso',
        'Pair up · not started': 'In coppia · da iniziare', 'Your Kern card': 'La tua Kern card',
        'What we saw, why, and your next move': 'Cosa abbiamo visto, perché e la tua prossima mossa',
        'Leave a sign': 'Lascia un segno', 'After each mission, leave one short tip for the next person.': 'Dopo ogni missione, lascia un breve consiglio per la persona dopo di te.',
        'Your answer is saved. Add another one anytime.': 'La tua risposta è salvata. Ne puoi aggiungere altre quando vuoi.',
        'Saved. This sharpens your KERN.': 'Salvato. Questo affina il tuo KERN.',
        "Noted. We'll give this guess less weight.": 'Preso nota. Daremo meno peso a questa ipotesi.',
      };
      let kLang = 'en';
      const tr = (s: string) => (kLang === 'it' && IT[s]) || s;
      const kTxt = kScr.querySelectorAll<HTMLElement>('.k-l, h4, h5, p, .k-sig, .k-chips span, .k-done span, .k-pill, .k-tag span, .k-tag strong, .k-ask button, .k-btn, .k-tabs button:not(:nth-child(2))');
      kTxt.forEach((e) => { e.dataset.en = e.innerHTML.trim(); });
      const setLang = (l: string) => {
        kLang = l;
        kTxt.forEach((e) => { const en = e.dataset.en!; if (en) e.innerHTML = tr(en); });
        if (fb.dataset.src) fb.textContent = tr(fb.dataset.src);
        document.querySelectorAll<HTMLElement>('[data-k-lang]').forEach((b) => b.setAttribute('aria-pressed', String(b.dataset.kLang === l)));
      };
      document.querySelectorAll<HTMLElement>('[data-k-lang]').forEach((b) => b.addEventListener('click', () => setLang(b.dataset.kLang!)));
      document.querySelectorAll<HTMLElement>('[data-k-theme]').forEach((b) => b.addEventListener('click', () => {
        kScr.classList.toggle('light', b.dataset.kTheme === 'light');
        document.querySelectorAll<HTMLElement>('[data-k-theme]').forEach((x) => x.setAttribute('aria-pressed', String(x === b)));
      }));
            const setAna = (l: string) => {
        document.documentElement.dataset.ana = l;
        document.querySelectorAll<HTMLElement>('[data-ana-lang]').forEach((b) => b.setAttribute('aria-pressed', String(b.dataset.anaLang === l)));
      };
      document.querySelectorAll<HTMLElement>('[data-ana-lang]').forEach((b) => b.addEventListener('click', () => setAna(b.dataset.anaLang!)));
      if (!navigator.language.toLowerCase().startsWith('it')) setAna('en');
</script>
